#include <stdio.h>

int main(void)
{
    int x = 100;
    _Complex y;
    y = 200;

    printf("one hundred in decimal is %d\nand in octal is %o\nand in hexadecimal is %X\n\n", x, x, x);
    printf("%d", y);


}
